# Contributing — mist-datasource

本仓文档遵循统一规范，canonical 入口：

- **主规范**：[`mist/docs/governance/documentation-guide.md`](../mist/docs/governance/documentation-guide.md)（以 workspace 根 `mist/` 计）
- **模板与工具**：[`mist/docs/readme-governance/`](../mist/docs/readme-governance/)

## 本仓特殊注意

- 涉及 TDX/QMT provider 字段或 WebSocket 帧时，README 不复述完整字段表，指向 `references/` 与 `openspec`。
- `uv` / `ruff` / `pyright` 的版本与命令以 `pyproject.toml` 与 `README` 为准。

改动 README 前按主规范 §9 自检，双语与 Badge 按 §3–§4 执行。
